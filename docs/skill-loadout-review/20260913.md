# 技編成の追従表示と操作保持

開始main: 23c89b32f01d951ab99291b9cc91bcf0f290049c（PR #180まで統合）。
PR #178のチュートリアル、#179/#180の戦闘改善を維持。2026-09-13にmain再取得し同一と確認。
専用ブランチ: fix/skill-loadout-status-20260913。ユーザー指定によりマージ・GitHub Pages公開は行わない。

## 計画と実装

1. 既存の対象カードを約86pxの追従サマリーへ置換。対象名・使用/上限・残り・技枠/3を数値で表示。上部バーの実測高さへ追従。
2. 装備不可理由を併記。コストは正確な不足量、枠満杯は解除、在庫は使用中/未所持を区別する。
3. 装備・解除時は見えているカードの位置を復元。カードが絞り込みから消える場合は次の残存カード、候補がない場合は元のスクロール座標（ページ末尾でブラウザがクランプ）を使用。
4. 検索・属性・コスト・装備可能のみ・検索欄の開閉状態を維持。
5. 技編成だけチュートリアル案内の配置領域から追従サマリーと下部ナビを除外。対象ボタンはサマリー直下に揃える。画面切替アニメーションによる一時的な位置ずれも除去。

変更ファイル: js/skills.js, js/tutorial.js, css/ui-redesign.css, index.html, js/notices-data.js。
確認用: scripts/skill-loadout-browser.html, scripts/skill-loadout-fixture.js（本編では未読込）。

## 検証結果

- npm run check（postcheck含む）成功。git diff --check、変更JS構文確認成功。
- Cloud Chromeの実ページiframeを320×568、360×640、412×915、844×390に設定し、各22項目成功。
- 技枠3/3、満杯理由、上限ちょうど、残り0、不足量、装備成功、装備後のカード位置差2px未満、属性フィルター/開閉維持、解除成功、解除後の位置差2px未満、数値即時更新、上下バー非重複、保存失敗時ロールバック、使用中カードの枚数不足、装備可能のみ、検索/コスト条件保持、検索0件、長名横溢れなし、案内と上下バー非重複、解除ボタン遮蔽なし、チュートリアル解除→装備→属性案内。
- テスト用仲間・技のみをメモリー上に配置し、saveGameの書込みを無効化。本番セーブには触れない。
- 最後の1枚を外すと初期技が補充される既存仕様は維持。コスト・技枠・装備条件・所持枚数・セーブキー/形式の変更なし。

## 制約と実機確認

Android/Chromebook実機は未実施。ブラウザ幅設定の合格を実機合格とは扱わない。
長い名前は1行省略（完全な名前はDOM・titleに保持）。文字拡大とAndroidキーボード表示中の確認は未実施。
保存/再読込は既存の保存移行テストで確認。本変更のブラウザQAは書込み無効のため、実セーブでの再読込は未確認。

## スマホ確認版

https://monster-rpg-skills-review-0913.kanki-doi-2.chatgpt.site/scripts/skill-loadout-browser.html

通常・満杯・上限・不足・長名・0件・チュートリアルをボタンで選択可能。本人限定。本番とは別オリジン。
